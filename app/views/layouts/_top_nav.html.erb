<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Jungle</a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li>
          <%= link_to "Home", :root %>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            Categories
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu">
            <% Category.all.each do |category| %>
              <li><%= link_to category.name, category %></li>
            <% end %>
          </ul>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
       <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            <span class="text-danger"><%= fa_icon "lock", text: "Admin" %></span>
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to "Dashboard", [:admin, :root] %></li>
            <li><%= link_to "Products",  [:admin, :products] %></li>
            <li><%= link_to "Categories", [:admin, :categories] %></li>
          </ul>
        </li>
        <% if current_user %>
          <li class="nav navbar-nav signed-in">
            <a class="signed-in">Signed In As: <%= current_user.email %></a>
          </li>
          <li class="nav navbar-nav logout">
            <a class="logout" href="/logout"><%= fa_icon "sign-out" %> Logout</a>
          </li>
        <% else %>
          <li class="nav navbar-nav login">
            <a class="login" href="/login"><%= fa_icon "lock" %> Login</a>
          </li>
          <li class="nav navbar-nav register">
            <a class="register" href="/register"><%= fa_icon "user-plus" %> Register</a>
          </li>
        <% end %>
        <li>
          <a class="<%= cart.size == 0 ? "empty-cart" : "filled-cart"%>" href="/cart"
            data-test-id="cart-btn">
            <%= fa_icon "shopping-cart" %> My Cart (<%= cart.size %>)
          </a>
        </li>
      </ul>
    </div>
  </div>
</nav>